on: push
name: 🚀 Deploy and Publish website on push
jobs:
  web-deploy:
    name: 🎉 Deploy and Publish
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4xxxxxxx

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5xxxxxx
      with:
        server: 79.143.89.216
        username: root
        password: ${{ secrets.FP-BTSD_Password }}
        protocol: ftps
        port: 43522 
